
server:
  servlet:
    context-path: /api/v1
      
config:
  activate:
    on-profile: integration-test

logging:
  level:
    root: INFO  # Desliga todos os logs

features:
  path: src/test/resources/features
